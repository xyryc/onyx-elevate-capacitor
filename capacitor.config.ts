import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.onyxelevate.app",
  appName: "Onyx Elevate",
  webDir: ".output/public",
};

export default config;
